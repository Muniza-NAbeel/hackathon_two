apiVersion: v1
kind: Service
metadata:
  name: todo-frontend
  labels:
    app: frontend
spec:
  type: NodePort
  selector:
    app: frontend      # ✅ sirf ye label match karega
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30000
      protocol: TCP
