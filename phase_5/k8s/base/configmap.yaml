apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: todo-app
data:
  DATABASE_URL: "postgresql+asyncpg://postgres:postgres@postgres:5432/todoapp"
  DAPR_HTTP_PORT: "3500"
  DAPR_PUBSUB_NAME: "kafka-pubsub"
  TASK_EVENTS_TOPIC: "task-events"
  REMINDERS_TOPIC: "reminders"
  EVENTS_ENABLED: "true"
  BACKEND_APP_ID: "chat-backend"
  LOG_LEVEL: "INFO"
